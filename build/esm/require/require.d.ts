/** The native `require` function, usable from both the CJS and the ESM build */
export declare const require: NodeRequire;
/** The directory this helper is located in after the build (`build/esm/require` or `build/cjs/require`) */
export declare const dirName: string;
